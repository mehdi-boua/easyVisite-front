import { CompAnchorDirective } from './comp-anchor.directive';

describe('CompAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new CompAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
